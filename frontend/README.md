# Credit Management Frontend

This is a Vite + React (TypeScript) frontend that talks to the Spring Boot backend at `http://localhost:8080`.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server proxies `/api` to the backend.
